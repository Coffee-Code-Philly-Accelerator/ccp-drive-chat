[project]
name = "ccp-drive-chat"
version = "0.1.0"
requires-python = "~=3.13.0"
dependencies = [
    "blaxel[openai]>=0.4.1",
    "composio==0.17.1",
    "dispatch-agents==0.14.4",
    "mcp<2",
]

[tool.uv]
package = false
